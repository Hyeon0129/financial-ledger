# My Ledger - 프로젝트 개요 및 세션 작업 기록

## 1. 프로젝트 개요 및 배경
- **프로젝트명:** My Ledger (개인용 가계부 애플리케이션)
- **배경:** 수입/지출, 예산, 목표 저축, 대출 및 정기 결제(구독 등)를 종합적으로 관리하기 위한 프리미엄 웹/데스크톱 가계부.
- **특징:** 'Liquid Glass' 효과(어두운 딥 블루 배경, 반투명한 블러 카드, SVG 난류 필터 등)를 사용한 세련된 모던 UI 및 UX.

## 2. 기술 스택
- **Frontend:** React 19, TypeScript, Vite, Recharts (차트 시각화)
- **Backend/DB:** Node.js (Express), SQLite (`better-sqlite3`)
- **Desktop:** Electron (향후 크로스 플랫폼 데스크톱 앱 패키징 대비)

---

## 3. 세션 작업 내역 (상세 기록)

이번 세션에서는 클라우드(Supabase)로 이관되었던 데이터를 다시 로컬(SQLite) 환경으로 무손실 전환하고, 전반적인 앱의 UI/UX 완성도를 상용 프로덕트 수준으로 극대화하는 작업들을 진행했습니다.

### 3.1. 백엔드 구조 원복 및 고도화 (Supabase ➡️ SQLite)
- **무손실 마이그레이션:** 기존 Supabase 환경에서 추가되었던 비즈니스 로직(대출 이자 계산, 정기 결제, 영구 예산 등 최신 기능)을 로컬 SQLite 백엔드로 100% 무손실 연동.
- **API 클라이언트 복원:** `src/api.ts`를 재작성하여 클라우드 대신 로컬 백엔드(`/api/...`)와 직접 통신하도록 스위치. (기존 Supabase 연동 코드는 `*.supabase.ts` 형태로 안전하게 백업)
- **개발 환경 최적화:** `package.json`의 `dev` 스크립트를 수정하여 `concurrently`를 통해 백엔드 서버(3001)와 프론트엔드(5173)가 단일 터미널 명령어로 동시에 구동되도록 개선.
- **구동 에러 해결:** Node.js 버전 불일치로 인한 `better-sqlite3` 모듈 충돌 및 서버 구동 실패(ECONNREFUSED) 문제를 `npm rebuild`를 통해 로컬 환경에 맞춰 재빌드하여 완벽하게 해결.

### 3.2. UI / UX 및 레이아웃 전면 개선

#### 1) 대시보드 (Dashboard)
- **고정지출 테이블 일관성:** '최근 소비' 테이블의 디자인 언어를 차용하여 고정지출 테이블의 이중 밑줄 제거, 투명 배경, 컬럼 중앙 정렬 적용 등 시각적 일관성 확보.
- **커스텀 아이콘 렌더링:** 고정지출 항목(넷플릭스 등)에 대해 텍스트 앞글자 또는 사용자가 업로드한 커스텀 아이콘 이미지가 표에 직접 렌더링되도록 기능 추가. (이미지 렌더링 버그 `url("")` 래핑으로 해결)
- **전체 레이아웃 및 여백 최적화:**
  - 브라우저 창 내에서 의미 없는 전체 세로 스크롤바가 생기지 않도록 방지.
  - 대시보드의 그리드 높이를 정밀 조정(`height: calc(100vh - 128px)`)하여 메인 콘텐츠의 하단 라인이 사이드바 하단 라인과 단 1px의 오차 없이 일치하도록 교정.
  - 각 메인 타이틀('최근 소비', '고정지출') 하단의 여백(Margin)을 넓혀 답답함을 해소하고 숨통이 트이는 개방감 있는 레이아웃으로 변경.

#### 2) 카테고리 관리 (Categories)
- **레이아웃 전면 개편 (다단 구조):** 수입 카테고리와 지출 카테고리를 상하가 아닌 **5:5 좌우 비율**로 나누어 배치.
- **조적식(Masonry) 카드 레이아웃:** 우측 지출 구역에 다단(CSS Columns) 레이아웃을 적용, 대분류별로 나뉜 카드들이 카테고리 개수와 무관하게 빈 공간 없이 테트리스처럼 촘촘히 정렬되어 세로 스크롤 발생을 억제.
- **상호작용(UX) 개선:** 화면을 지저분하게 만들던 '작업(수정/삭제)' 버튼열을 완전히 제거. 대신 **카테고리 행 텍스트를 마우스로 직접 클릭**하면 즉시 수정/삭제 모달이 뜨는 모던한 클릭 상호작용 방식으로 전환.
- **시각적 계층화 (Hierarchy):**
  - 대분류와 소분류의 글꼴 크기/굵기를 차등 적용.
  - 소분류 항목 좌측에 허접한 키보드 특수문자(`|_`) 대신, 고품질의 **SVG 가지(Tree Branch) 연결선 아이콘**을 적용하여 직관적이고 고급스러운 트리 구조 시각화.
  - Hover 시 지저분한 배경색 대신, 텍스트가 살짝 튀어 오르는(`translateY(-2px)`) 애니메이션을 부여.

#### 3) 예산 관리 (Budgets)
- **예산 목록 정렬 로직 자동화:** 시간순으로 중구난방 섞여 있던 예산 목록을 카테고리 트리(대분류 ➡️ 소분류) 순서에 맞게 자동으로 그룹화 및 정렬되도록 알고리즘 개선.
- **드래그 앤 드롭 (Drag & Drop):** 예산 및 카테고리 목록에서 사용자가 항목을 드래그하여 원하는 순서대로 배치할 수 있도록 기능 추가. (순서는 LocalStorage에 영구 저장)
- **잔액 상태(Status) 직관화:** 불필요한 액션 버튼 공간을 제거하고, 그 자리에 예산 현황을 직관적으로 보여주는 상태 텍스트 적용. (예산 남음: `+ 파란색`, 초과: `- 빨간색`, 일치: `0`)
- **내부 스크롤바 추가:** 예산 항목이 많아질 경우 전체 페이지가 밀려나지 않도록 테이블 내부에만 스크롤바가 생기도록 컨테이너화.

#### 4) 계좌 및 대출 관리 (Accounts / Loans)
- 대출 관리 테이블 행에 드래그 앤 드롭 기능을 도입하면서 일시적으로 붕괴했던 그리드(Grid) 레이아웃을 완벽하게 원복 및 중앙 정렬 형태로 재교정.

#### 5) 전역 모달 및 설정 UI (Global Modals)
- **프로필 사진 저장 기능 정상화:** 
  - 백엔드 파일 업로드 용량을 상향(`limit: '50mb'`).
  - `PUT /api/user` 요청 시 `avatar_url` 필드를 정상적으로 DB에 UPDATE 하도록 SQL 쿼리 로직 보강 (Partial Update 적용).
- **실시간 프로필 동기화:** 저장하는 즉시 좌측 상단 사이드바와 우측 상단 프로필 버튼의 이미지가 유저 사진으로 동기화 렌더링되도록 최적화.
- **모달 스크롤바 시각적 은닉:** 모든 모달(새 거래, 카테고리 수정, 설정 등)에서 마우스 휠 스크롤 기능은 정상 작동하게 두면서, 보기 흉한 우측 세로 스크롤바 UI만 감쪽같이 숨기도록(`::-webkit-scrollbar { display: none; }`) 글로벌 CSS 조정. (이 과정에서 발생했던 뒷배경 흰색 사각형 버그도 `overflow` 재지정으로 완벽히 해소).
- **중복 UI 클렌징:** 각 뷰 상단이나 패널 안에 중복으로 나타나던 타이틀(`<h2>` 태그) 및 `Finance Overview` 등의 어색한 영문 서브타이틀을 모두 제거 및 한글화하여 시야를 넓힘.
