# 프로젝트 버그 및 논리 오류 리스트

## 1. 대출 (Loans) 관련 버그 및 논리 오류 (`api.ts`, `AccountsView.tsx`)

**🚨 1-1. 대출 조건(금리, 기간 등) 수정 시 월 상환액 미갱신 버그**
*   **현상/원인:** `AccountsView.tsx`의 대출 수정 화면에서 대출 기간(`term_months`)이나 이자율(`interest_rate`) 등을 변경하고 저장할 때, 다시 계산된 월 상환액(`monthly_payment`) 정보가 백엔드 업데이트 페이로드에 포함되지 않습니다. 
*   **영향:** 이자율이나 상환 기간을 변경하더라도 기존에 생성될 때 계산되었던 예전 월 상환액이 계속 청구됩니다. 결과적으로 대출금 자동 상환액이 잘못 출금되며 전체 대출 잔액과 기간이 완전히 꼬이게 됩니다.

**🚨 1-2. 대출 기간을 0개월로 입력 시 발생하는 무한 자동 상환 버그**
*   **현상/원인:** 대출 생성/수정 시 `term_months`를 0으로 입력하는 것을 허용하고 있습니다. 이 경우 계산된 월 상환액은 0원이 되며, `processLoanAutopayIfNeeded` (자동 상환 로직)에서 원금이 전혀 깎이지 않는 상환 처리가 발생합니다.
*   **영향:** 상환 루프의 종료 조건(잔액 소진)이 영원히 충족되지 않아 데이터베이스에 0원짜리 상환 내역이 끝없이 생성될 위험이 존재합니다.

**🚨 1-3. 중복 상환(Dedupe) 방지 키의 한계로 인한 당일 중복 상환 유실**
*   **현상/원인:** 수동으로 상환 처리(`settle`)를 할 때 중복 처리를 막기 위해 `SETTLE_LOAN|loanId|settledDate` 형태의 메모 값을 고유 키로 활용합니다. 여기서 날짜는 일(Day) 단위까지만 기록됩니다.
*   **영향:** 사용자가 하루에 2번 이상 부분 상환을 진행하려 할 경우, 시스템은 이미 당일 상환 내역이 있는 것으로 간주하여 두 번째부터의 지출(출금 트랜잭션)을 무시합니다. 그러나 대출 원금 자체는 깎이게 되어 자산 밸런스의 불일치가 발생합니다.

**🚨 1-4. 자동 상환 동시성 문제 (Race Condition)**
*   **현상/원인:** 대출 자동 상환 로직이 하루 1회만 동작하도록 로컬 스토리지 키(`guardKey`)를 활용합니다. 하지만 네트워크 요청(Supabase)이 완료된 이후에 로컬 스토리지에 오늘 날짜를 기록합니다.
*   **영향:** 브라우저 탭을 동시에 여러 개 열거나 렌더링이 빠르게 여러 번 일어나는 상황이 생기면, 방어 로직이 작동하기 전이라 다수의 탭에서 동시에 상환 로직이 실행되어 하루에 여러 달 치 대출금이 중복으로 빠져나가는 문제가 발생할 수 있습니다.

---

## 2. 고정지출 (Bills) 및 신용카드 로직 관련 결함 (`App.tsx`, `billsStore.ts`, `accountMetaStore.ts`)

**🚨 2-1. 앱 미접속 시 과거의 고정지출/카드 대금 자동 이체 영구 누락**
*   **현상/원인:** `App.tsx`의 메인 자동 이체 루프는 오직 **'현재 화면에서 조회 중인 달(Current Month)'과 '오늘 날짜'** 사이에 존재하는 청구일만 검사합니다.
*   **영향:** 사용자가 특정 달(예: 3월)을 통째로 건너뛰고 앱을 실행하지 않다가 다음 달(4월)에 실행하게 되면, 3월에 빠져나갔어야 할 고정지출과 신용카드 대금 트랜잭션은 과거 달의 범위에 속하므로 영구적으로 자동 생성되지 않습니다.

**🚨 2-2. 신용카드 대금 계산 시 환불(Income) 및 선결제(Transfer) 내역 무시**
*   **현상/원인:** 이번 달 카드 대금을 산정하는 로직이 단순히 `type === 'expense'` 인 결제 내역만 합산하도록 작성되어 있습니다.
*   **영향:** 카드를 결제 취소하여 발생한 환불액(`income`)이나, 카드 결제일 전에 미리 카드 대금을 상환한 이체 내역(`transfer`)이 반영되지 않아 사용자에게 실제 청구 금액보다 과도한 금액이 통장에서 출금된 것으로 기록됩니다.

**🚨 2-3. 신용카드 청구 주기(Cycle) 계산 논리 오류**
*   **현상/원인:** 청구 주기 로직에서 `cycleStartDay > cycleEndDay` 일 때만 전월~이번 달 범위로 인식합니다. 
*   **영향:** 만약 사용자가 결제 주기를 '전월 1일 ~ 전월 말일' 형태로 세팅한다면, `1 < 31` 이 되므로 로직상 시작 월이 전월로 넘어가지 않고 **이번 달**로 묶이게 됩니다. 청구액 대상이 완전히 틀어지게 됩니다.

**🚨 2-4. 고정지출 식별자 훼손 시 발생하는 무한 중복 결제**
*   **현상/원인:** 고정지출 트랜잭션은 메모란에 `AUTO_BILL|...`와 같은 특정 문자열을 남겨 중복 이체를 방지합니다. 
*   **영향:** 만약 사용자가 [거래 내역] 화면에서 이 메모를 지우거나 임의로 수정하게 되면 중복 방지 식별자가 파괴됩니다. 다음번 앱이 렌더링될 때 시스템은 해당 지출이 없다고 판단하여 똑같은 고정 지출을 다시 생성하는 무한 루프가 발생합니다.

**🚨 2-5. 삭제 로직 연쇄 작용으로 인한 이전 거래내역 데이터 훼손**
*   **현상/원인:** [고정지출] 탭에서 특정 고정지출 항목을 삭제할 때, 시스템이 그 고정지출과 연결되어 있던 카테고리(`category_id`) 자체를 데이터베이스에서 함께 지워버립니다.
*   **영향:** 사용자가 해당 카테고리를 일반 지출에도 공통으로 사용하고 있었다면, 고정지출을 삭제하는 순간 멀쩡한 다른 과거 지출 내역들의 카테고리 정보가 전부 빈값(Uncategorized)으로 날아가게 됩니다.
